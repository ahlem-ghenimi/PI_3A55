<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails du Produit</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        h1 {
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Détails du Produit</h1>
    <table>
        <tr>
            <th>ID</th>
            <th>Nom du Produit</th>
            <th>Cycle de Culture</th>
            <th>Quantité Vendue</th>

            <th>Quantité</th>
            <th>Unité</th>
            <th>Date de Semis</th>
            <th>Date de Récolte</th>
            <th>Categorie</th>
            <th>Fournisseur</th>
        </tr>
        <tr>
            <td>{{ produit.id }}</td>
            <td>{{ produit.nomProduit }}</td>
            <td>{{ produit.getCytcleCulture() }}</td>
            <td>{{ produit.getQuantiteVendue() }}</td>

            <td>{{ produit.getQuantiteProduit() }}</td>

            <td>{{ produit.getUniteQuantProd() }}</td>

            <td>{{ produit.getDateSemisProd() ? produit.getDateSemisProd()|date('Y-m-d') : '' }}</td>

            <td>{{ produit.getDateRecolteProd() ? produit.getDateRecolteProd()|date('Y-m-d') : '' }}</td>

            <td>{{ produit.getCategorie() ? produit.getCategorie().getNomCategorie() : 'N/A' }}</td>

            <td>{{ produit.getFournisseur() ? produit.getFournisseur().getNomFournisseur() : 'N/A' }}</td>


        </tr>
    </table>
</body>
</html>
